Kindly download the py file (car_showroom,SQL_DATABASE)
Then save it in new folder in your system
THen download the png file and save it into a folder name it as Images(It is compalsory to name it as Images)
than save this the file together in one folder(any name)[Images,car_showroom.py,SQL_DATABASE.py]
Before running the py file download the required modules for py files(see then in file before running).
